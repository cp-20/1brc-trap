{
  "name": "@1brc/runner",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun src/gateway.ts",
    "build": "tsc -p tsconfig.json && esbuild src/gateway.ts --bundle --platform=node --format=esm --outfile=dist/gateway.js",
    "start": "bun dist/gateway.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run src",
    "test:coverage": "vitest run src --coverage --coverage.include='src/**/*.ts' --coverage.exclude='src/**/*.test.ts' --coverage.reporter=text --coverage.reporter=json-summary --coverage.thresholds.lines=8"
  },
  "dependencies": {
    "@1brc/domain": "workspace:*",
    "execa": "^9.6.1",
    "neverthrow": "^8.2.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "vitest": "^4.1.10"
  }
}
